# 星界之律｜手機玩家分享指南

## 最簡單的分享方式：靜態網頁版

這個模式適合先讓朋友用手機遊玩，不需要你自己的電腦一直開著伺服器。

1. 上傳整個 `gacha-system` 資料夾到支援靜態網站的託管服務。
2. 確認 `index.html` 位於網站根目錄，且 `src`、`assets`、`styles.css` 也一起上傳。
3. 取得託管服務提供的 `https://...` 網址。
4. 把網址傳給玩家；手機瀏覽器開啟後就能進入登入頁與遊戲大廳。

不要只傳 `index.html`，否則角色圖片、背景與遊戲程式會缺檔。

靜態版會把每位玩家的帳號與進度存在「自己的手機瀏覽器」中：同一位玩家在同一台手機、同一個瀏覽器可以繼續遊玩；換手機、清除網站資料或換瀏覽器後不會自動帶過去。這種模式不需要後端，也不需要你開啟 Codex 伺服器，但製作者不能從後端查看或修改玩家資料。

## 先給同一個 Wi-Fi 的手機測試

如果只是要讓身邊的人先用手機測試，不必先買或申請公開網址：

1. 在電腦的 `gacha-system` 資料夾開 PowerShell。
2. 執行 `$env:HOST="0.0.0.0"; node serve.mjs`。
3. 在電腦執行 `ipconfig`，找出目前 Wi-Fi 的 IPv4，例如 `192.168.1.25`。
4. 手機連到同一個 Wi-Fi，開啟 `http://192.168.1.25:8080/`。

這只是同一個區域網路的測試網址，電腦關機或停止程式後就不能使用；若 Windows 防火牆詢問是否允許 Node 通過私人網路，測試時允許即可。

## 需要集中管理玩家資料時

如果希望玩家換裝置仍能用遊戲名稱與密碼載入進度，或希望製作者能查詢、補發星砂、調整保底，就要把整個專案部署成 Node 網站，而不是只放靜態檔案。

1. 將整個 `gacha-system` 部署到支援 Node.js 的託管服務。
2. 啟動命令使用 `node serve.mjs`，平台提供的 `PORT` 會自動使用。
3. 正式環境設定 `NODE_ENV=production` 與自己的 `STARSHIP_ADMIN_KEY`。
4. 確認服務有持久化磁碟或資料庫，讓 `data/players.json` 不會在服務重啟後消失。
5. 取得 HTTPS 網址後，玩家與製作者都使用同一個網址；玩家頁是 `/`，管理頁是 `/admin.html`。

目前程式的前端與後端同源運作。若未來把前端與後端放在兩個不同網址，需要再加 API 網址設定與 CORS 驗證，不能只把前端檔案搬過去。

## Render 部署範例

專案根目錄已附上 `render.yaml`。Render 建立 Blueprint 時會讀取這份設定：服務使用 Node runtime，啟動命令是 `node serve.mjs`，健康檢查是 `/api/health`，並將玩家資料目錄設為 `/opt/render/project/src/storage/data`。

如果你使用 Render：

1. 先把整個 `gacha-system` 放到你自己的 GitHub 儲存庫，或在 Render 建立 Web Service 時連結你的儲存庫。
2. 建立 Blueprint / Web Service，使用專案內的 `render.yaml`。若用手動建立，Build Command 填 `npm install`，Start Command 填 `node serve.mjs`。
3. 在初次建立時設定 `STARSHIP_ADMIN_KEY`；請自行產生一組長密鑰，不要把密鑰傳給別人或寫進檔案。
4. 若使用目前的 Postgres 版本，設定 `DATABASE_URL`，資料會寫入 Postgres，不再依賴 `data/players.json`；若沒有資料庫連線，才需要掛載到 `/opt/render/project/src/storage` 的 Persistent Disk。
5. 部署完成後使用同一個 HTTPS 網址：玩家開 `/`，製作者開 `/admin.html`。

Render 的一般檔案系統在重啟或重新部署時不是玩家存檔的可靠保存位置。現在伺服器支援 Postgres，設定 `DATABASE_URL` 後會自動建立 `starship_players` 資料表；玩家數量或同時在線量變大後，再進一步加入正式資料庫備份、連線池與遷移流程。

## 手機測試注意

- 建議使用手機 Chrome、Safari 或 Edge 開啟 HTTPS 網址。
- 玩家第一次登入時要記住遊戲名稱與密碼；靜態版的存檔只存在該手機瀏覽器。
- 更新網頁程式時不要刪除玩家的網站資料；後端模式更新前先備份 `data/players.json`。
- 本次大更新開放玩家可領獎劇情 1.0–2.5，並新增 2.0–2.5 限定卡池與星港委託；3.0–4.5 劇情與角色已建檔但仍鎖定，部署時不要誤加入現行卡池。
- 更新後每個帳號（含新帳號）登入時一次領取 3,200 星砂；星界試煉 30 關的挑戰與獎勵次數也會在這次版本重置。
